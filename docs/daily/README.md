# Daily Legacy Workflow

This directory preserves the older daily `5`-trading-day workflow as a historical comparator.

Use this namespace when you need to answer:

- what the frozen daily baseline was
- which config reproduces it
- which artifact is the historical comparison anchor

Do not treat the daily workflow as the repo default. The live workflow is documented under `docs/quarterly/`.
