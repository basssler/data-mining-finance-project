# Quarterly Canonical Configs

This directory is the canonical namespace for quarterly workflow manifests, indexes, and future dedicated quarterly config packages.

Current live references:

- Baseline quarterly config: `../event_panel_v2_quarterly_63d_sector_relative.yaml`
- Active candidate config: `../event_panel_v2_quarterly_stability_core_additive.yaml`
- Benchmark ladder manifest: `experiments/benchmark_ladder.yaml`
- Registry doc: `../../docs/quarterly/promoted_models.md`

Use the root-level `event_panel_v2_quarterly*.yaml` files as the executable configs until they are fully moved into this namespace. The old `../event_panel_v2_quarterly.yaml` file is a legacy 21-day benchmark retained only for historical comparison.
