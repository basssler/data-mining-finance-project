# Daily Legacy Configs

This directory indexes legacy daily benchmark configs.

- Frozen baseline config copy: `legacy_event_panel_v2_primary.yaml`
- Live quarterly config namespace: `../quarterly/`

Keep daily configs here only when they are historical comparators or compatibility copies.
